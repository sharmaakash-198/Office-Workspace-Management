import { visibleBounds } from '../svg/analyzer.js'; import { validateGeometry } from './validator.js'; import type { Asset, Geometry, Mode } from '../types/geometry.js';
export function generateAsset(source:string, svg:string, mode:Mode='auto'):Asset { const id=source.replace(/\\/g,'/').replace(/\.svg$/i,'').replace(/[^\w/-]+/g,'-'),label=source.split('/').pop()?.replace(/\.svg$/i,'')??id; try {const b=visibleBounds(svg);if(!b||b.width<=0||b.height<=0)return{id,source,label,metadata:{automatic:true,status:'REVIEW_REQUIRED',reason:'No visible positive-area geometry'}}; const r=b.height/b.width,w=r<=1?1:+(1/r).toFixed(6),h=r<=1?+r.toFixed(6):1; const geometry:Geometry=mode==='orthogonal'?{type:'orthogonal-polygon',points:[[0,0],[w,0],[w,h],[0,h]]}:{type:'rectangle',x:0,y:0,width:w,height:h};const bad=validateGeometry(geometry);return{id,source,label,geometry,metadata:{automatic:true,status:bad?'REVIEW_REQUIRED':mode==='orthogonal'?'AUTO_ORTHOGONAL':'AUTO_BOUNDS',reason:bad,bounds:b}}}catch(e){return{id,source,label,metadata:{automatic:true,status:'FAILED',reason:e instanceof Error?e.message:String(e)}}} }
