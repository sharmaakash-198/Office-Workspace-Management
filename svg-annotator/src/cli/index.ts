#!/usr/bin/env node
import { promises as fs } from 'node:fs'; import path from 'node:path';
import { generateAsset } from '../geometry/generate.js'; import { createCatalog } from '../catalog/generator.js'; import type { Asset, Mode } from '../types/geometry.js';
const argv=process.argv.slice(2); if(argv.includes('--help')||argv.length<2){console.log('Usage: svg-shape-mapper <input> <output> [--mode rectangle|orthogonal|auto] [--force] [--report]');process.exit(argv.length<2?1:0)} if(argv.includes('--version')){console.log('1.0.0');process.exit(0)}
const input=path.resolve(argv[0]),output=path.resolve(argv[1]), mi=argv.indexOf('--mode'), mode:Mode=(mi>=0?argv[mi+1]:'auto') as Mode; if(!['rectangle','orthogonal','auto'].includes(mode)){throw Error('Invalid --mode')}
async function files(dir:string):Promise<string[]>{const entries=await fs.readdir(dir,{withFileTypes:true});return (await Promise.all(entries.map(e=>e.isDirectory()?files(path.join(dir,e.name)):e.isFile()&&e.name.toLowerCase().endsWith('.svg')?[path.join(dir,e.name)]:[]))).flat()}
const list=await files(input); const assets:Asset[]=[]; console.log(`Processing ${list.length} SVG files…`); for(let i=0;i<list.length;i++){const file=list[i],rel=path.relative(input,file).split(path.sep).join('/');let a:Asset;try{a=generateAsset(rel,await fs.readFile(file,'utf8'),mode)}catch(e){a={id:rel,source:rel,metadata:{automatic:true,status:'FAILED',reason:String(e)}}}assets.push(a);const target=path.join(output,'shapes',rel.replace(/\.svg$/i,'.json'));await fs.mkdir(path.dirname(target),{recursive:true});await fs.writeFile(target,JSON.stringify(a,null,2)+'\n');if((i+1)%100===0||i+1===list.length)console.log(`${i+1}/${list.length}`)}
await fs.mkdir(output,{recursive:true});const catalog=createCatalog(assets,mode);await fs.writeFile(path.join(output,'catalog.json'),JSON.stringify(catalog,null,2)+'\n');const failed=assets.filter(a=>a.metadata.status==='FAILED'),review=assets.filter(a=>a.metadata.status==='REVIEW_REQUIRED');const report={total:assets.length,successful:assets.length-failed.length-review.length,reviewRequired:review.length,failed:failed.length,errors:assets.filter(a=>a.metadata.reason).map(a=>({source:a.source,status:a.metadata.status,reason:a.metadata.reason}))};await fs.writeFile(path.join(output,'report.json'),JSON.stringify(report,null,2)+'\n');console.log(`Complete: ${report.successful} successful, ${report.reviewRequired} review, ${report.failed} failed`);
